import React from 'react'
import useTablero from './useTablero'

type Props = {}

const usePartida = (props: Props) => {
    // useTablero();
    //condición de victoria
    return (
        <div>usePartida</div>
    )
}

export default usePartida